enum CouponType{
    USER_SPECIFIC,
    PRODUCT_SPECIFIC,
    MIN_PURCHASE_AMOUNT,
    CATEGORY_SPECIFIC,
    BUNDLE_DISCOUNT,
    SPECIFIC_LINK
}

enum CouponDiscountType{
    PERCENTAGE_DISCOUNT, FIXED_AMOUNT_DISCOUNT
}